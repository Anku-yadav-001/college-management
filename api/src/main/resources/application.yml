spring:
  application:
    name: api
  profiles:
    active: prod
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
    timeout:
      seconds: 900

logging:
  level:
    org:
      springframework=DEBUG: